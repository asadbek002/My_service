'use client';
import Link from 'next/link';import{useEffect,useState}from'react';import{useRouter}from'next/navigation';import{api}from'../lib/api';
type Payment={id:string;kind:string;amount:string;method:string;reason?:string;createdAt:string;order:{number:string;status:string;customer:{firstName:string;phone:string}}};
export default function Payments(){const router=useRouter();const[x,setX]=useState<Payment[]>([]);const[e,setE]=useState('');useEffect(()=>{api<Payment[]>('/payments').then(setX).catch(v=>v instanceof Error&&v.message==='SESSION_EXPIRED'?router.replace('/login'):setE(v instanceof Error?v.message:'Xato'));},[]);const net=x.reduce((n,v)=>n+(v.kind==='REFUND'?-1:1)*Number(v.amount),0);return <main className="page"><header><Link href="/dashboard" className="brand">MY SERVICE</Link><Link href="/reports">Hisobot</Link></header><p className="eyebrow">MOLIYA</p><h1>To‘lovlar</h1>{e&&<p className="error">{e}</p>}<div className="cards"><section><span className="muted">Sof to‘lov</span><strong>{net.toLocaleString('uz-UZ')}</strong></section><section><span className="muted">Operatsiyalar</span><strong>{x.length}</strong></section></div><section><div className="table-scroll"><table><thead><tr><th>Sana</th><th>Buyurtma</th><th>Mijoz</th><th>Turi</th><th>Usul</th><th>Summa</th></tr></thead><tbody>{x.map(v=><tr key={v.id}><td>{new Date(v.createdAt).toLocaleString('uz-UZ')}</td><td>{v.order.number}</td><td>{v.order.customer.firstName}<small>{v.order.customer.phone}</small></td><td>{v.kind}</td><td>{v.method}</td><td className={v.kind==='REFUND'?'amount-negative':''}>{v.kind==='REFUND'?'-':'+'}{Number(v.amount).toLocaleString('uz-UZ')}</td></tr>)}</tbody></table></div></section></main>}
